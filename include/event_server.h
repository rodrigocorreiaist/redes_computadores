#ifndef EVENT_SERVER_H
#define EVENT_SERVER_H

// This file is now mostly empty as we moved to storage.h and protocol handlers.
// We keep it for potential future global server definitions.

#endif // EVENT_SERVER_H
